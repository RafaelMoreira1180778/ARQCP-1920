#include <stdio.h>
#include "conta.h"

int i;

int main(void) {

	printf("Insira o valor de i: ");
	scanf("%d",&i);

	printf("Valores pré-definidos em Assembly: ");
	printf("A: %d, B: %d, n: %d\n", a, b, n);
	
	printf("Somatorio de i até N (vezes) de (i²*A²/B) = %d: 0x%x\n", steps(), steps());

	return 0;
}
