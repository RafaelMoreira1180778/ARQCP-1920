#include <stdio.h>
#include "conta.h"

int num=0;

int main(void) {

	printf("Insira um valor para iniciar o cálculo:");
	scanf("%d",&num);

	printf("num*3+6:3+12-num-1 = %d: 0x%x\n", steps(), steps());

	return 0;
}
